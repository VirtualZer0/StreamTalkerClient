# StreamTalkerClient

🌐 **Язык:** [English](../../README.md) | Русский

**Десктопное приложение для озвучивания сообщений чата Twitch и VK Play с помощью AI-голосов**

---

## 📖 Описание

StreamTalkerClient — это кроссплатформенное десктопное приложение, которое озвучивает сообщения чата Twitch и VK Play Live с помощью AI-голосов. Оно подключается к чату вашего стрима, ставит входящие сообщения в очередь, отправляет их на TTS-сервер (синтез речи) и воспроизводит полученное аудио через динамики.

**Как это работает:** Подключение к чату → Сообщения ставятся в очередь → AI синтезирует речь → Аудио воспроизводится

**Поддерживаемые платформы:**
- Windows 10/11 (64-bit)
- Linux (окружение рабочего стола X11)

---

## 🚀 Установка

### Шаг 1: Скачайте приложение

1. Перейдите на [страницу релизов](https://github.com/VirtualZer0/StreamTalkerClient/releases)
2. Скачайте архив для вашей платформы:
   - **Windows:** `StreamTalkerClient-win-x64.zip`
   - **Linux:** `StreamTalkerClient-linux-x64.tar.gz`
3. Распакуйте архив в любую папку

### Шаг 2: Установите .NET Runtime

StreamTalkerClient требует .NET 10 Runtime — бесплатный компонент от Microsoft, необходимый для запуска приложения.

**Windows:**
1. Перейдите на [страницу загрузки .NET 10 Runtime](https://dotnet.microsoft.com/en-us/download/dotnet/10.0)
2. В разделе **".NET Desktop Runtime"** нажмите на установщик **x64** для Windows
3. Запустите скачанный установщик
4. Нажмите **Install** → дождитесь завершения → нажмите **Close**
5. Перезагрузите компьютер (рекомендуется)

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install dotnet-runtime-10.0
```

**Linux (Fedora):**
```bash
sudo dnf install dotnet-runtime-10.0
```

### Шаг 3: Настройте TTS-сервер

StreamTalkerClient нуждается в TTS-сервере для синтеза речи. Есть два варианта:

- **Запустить сервер локально** — Требуется видеокарта NVIDIA с 8 ГБ+ видеопамяти. См. [руководство по установке StreamTalkerServer](https://github.com/VirtualZer0/StreamTalkerServer#-установка)
- **Подключиться к удалённому серверу** — Если кто-то уже хостит сервер, вам нужен только его URL (например, `http://192.168.1.100:7860`)

### Шаг 4: Первый запуск

1. Запустите `StreamTalkerClient.exe` (Windows) или `StreamTalkerClient` (Linux)
2. Мастер настройки проведёт вас через:
   - **Выбор сервера** — Локальный Docker-сервер или ввод URL удалённого сервера
   - **Настройка Docker** (только для локального) — Приложение проверит Docker/WSL и скачает необходимые файлы
   - **Конфигурация ресурсов** (только для локального) — Установка лимитов памяти GPU и параметров модели
   - **Проверка подключения** — Проверяет, что TTS-сервер запущен и доступен
   - **Загрузка голосов** — Скачивает список доступных голосов с сервера

### Шаг 5: Подключение к чату

1. Выберите вкладку платформы (Twitch или VK Play)
2. Введите название канала
3. Нажмите **Подключиться**
4. Сообщения чата начнут появляться в очереди и воспроизводиться как аудио!

---

## ❓ FAQ

<details>
<summary><b>Приложение не запускается или вылетает при запуске</b></summary>

Скорее всего, не установлен .NET 10 Runtime. Скачайте и установите его со [страницы загрузки .NET](https://dotnet.microsoft.com/en-us/download/dotnet/10.0) — выберите **".NET Desktop Runtime"** для Windows. После установки перезагрузите компьютер.
</details>

<details>
<summary><b>Не удаётся подключиться к TTS-серверу</b></summary>

- Убедитесь, что TTS-сервер запущен (проверьте `http://localhost:7860/health` в браузере)
- Если используете удалённый сервер, проверьте правильность URL и доступность сервера
- Проверьте настройки файрвола — порт 7860 должен быть открыт
- Попробуйте перезапустить TTS-сервер
</details>

<details>
<summary><b>Нет звука</b></summary>

- Проверьте системную громкость и убедитесь, что выбрано правильное устройство вывода звука
- Проверьте ползунок громкости в приложении (панель очереди)
- Попробуйте очистить кеш (панель кеша → Очистить) и переподключиться
- На Linux убедитесь, что установлен `aplay` (`sudo apt install alsa-utils`)
</details>

<details>
<summary><b>Горячие клавиши не работают</b></summary>

- Убедитесь, что NumLock включён (горячие клавиши используют NumPad: NumPad5 = пропустить, NumPad4 = очистить)
- На Linux приложение требует X11 для глобальных горячих клавиш — Wayland не поддерживается
- Попробуйте запустить приложение с повышенными правами, если горячие клавиши всё равно не работают
</details>

<details>
<summary><b>Сообщения не появляются в очереди</b></summary>

- Проверьте, что вы подключены к правильному каналу
- Если в настройках включён `RequireVoice`, сообщения должны содержать тег голоса (например, `[название_голоса] текст сообщения`)
- Включите `ReadAllMessages` в настройках, чтобы обрабатывать все сообщения чата независимо от тегов голоса
</details>

<details>
<summary><b>Как изменить голос для сообщений</b></summary>

Два способа выбрать голос:
- **Для отдельного сообщения:** Укажите название голоса в сообщении через синтаксис скобок: `[название_голоса] Ваш текст`
- **Привязка к пользователю:** Перейдите в **Управление голосами** → вкладка **Привязки**, добавьте имя пользователя и назначьте голос. Все сообщения этого пользователя будут использовать привязанный голос.
</details>

<details>
<summary><b>Высокое потребление памяти</b></summary>

Приложение кеширует синтезированное аудио на диск. Если используется слишком много места:
- Уменьшите лимит размера кеша в настройках
- Нажмите **Сжать** на панели кеша для уменьшения размера
- Нажмите **Очистить** для удаления всех кешированных файлов
</details>

---

## 🔗 Связанные проекты

### [StreamTalkerServer](https://github.com/VirtualZer0/StreamTalkerServer)

AI-сервер синтеза речи на базе Qwen3-TTS, который озвучивает сообщения для StreamTalkerClient. Запускается локально через Docker на видеокарте NVIDIA или на удалённой машине.

---

## Возможности

- **Поддержка нескольких платформ** — Одновременная работа с чатами Twitch и VK Play Live
- **Настройка голосов** — Привязка голосов к пользователям и пользовательские параметры синтеза (скорость, температура, штраф за повторения)
- **Умная очередь** — Извлечение голоса из сообщений (синтаксис `[голос] текст` или режим первого слова), автоматическая дедупликация по ключу кеша
- **Локальный WAV-кеш** — Дисковый кеш с SHA256-ключами, вытеснение LRU и сжатие
- **Мастер первого запуска** — Пошаговая настройка локального Docker-сервера или подключения к удалённому TTS
- **Глобальные горячие клавиши** — Пропуск текущего/всех сообщений клавишами NumPad (кроссплатформенно через SharpHook)
- **Тёмная тема** — Fluent-дизайн с библиотекой иконок Semi Avalonia
- **Двуязычный интерфейс** — Переключение языка между английским и русским в рантайме

---

## Конфигурация

### Мастер первого запуска
При первом запуске StreamTalkerClient проводит вас через настройку сервера:

1. **Выбор сервера** — Выбор между локальным Docker-сервером или удалённым сервером
2. **Настройка Docker** (только для локального) — Проверка Docker/WSL, загрузка compose-файлов
3. **Конфигурация ресурсов** (только для локального) — Установка лимитов памяти GPU и оптимизаций модели
4. **Проверка работоспособности** — Проверка подключения к TTS-серверу
5. **Загрузка голосов** — Получение доступных голосов с сервера

### Ручная настройка
Настройки хранятся в `data/settings.json` и могут быть отредактированы вручную:

```json
{
  "Services": {
    "Twitch": {
      "Channel": "имя_вашего_канала",
      "ReadAllMessages": false,
      "RequireVoice": true
    }
  },
  "Voice": {
    "DefaultVoice": "female_calm",
    "VoiceExtractionMode": "bracket",
    "Speed": 1.0,
    "Temperature": 0.7
  },
  "Server": {
    "BaseUrl": "http://localhost:7860",
    "Language": "Russian"
  }
}
```

---

## Использование

### Подключение к чату
1. Выберите вкладку платформы (Twitch или VK Play)
2. Введите название канала
3. Нажмите **Подключиться**
4. Загрузите награды (опционально) для фильтрации по покупкам баллов канала

### Синтаксис голоса
Два режима извлечения голоса из сообщений:

**Режим скобок (по умолчанию):**
```
[название_голоса] Текст для синтеза
```

**Режим первого слова:**
```
название_голоса Текст для синтеза
```

### Привязки голосов
Назначение определённых голосов пользователям:
1. Нажмите **Управление голосами** → вкладка **Привязки**
2. Добавьте имя пользователя и выберите голос
3. Сообщения пользователя будут автоматически использовать назначенный голос

### Управление очередью
- **Ползунок громкости** — Глобальная громкость (0-100%) с переопределениями для отдельных голосов
- **Задержка воспроизведения** — Секунды между сообщениями (по умолчанию: 5с)
- **Пропустить текущее** — NumPad5 или нажатие кнопки
- **Очистить очередь** — NumPad4 или нажатие кнопки

---

## Структура проекта

```
StreamTalkerClient/
├── src/
│   ├── ViewModels/          # MVVM view models (MainWindowViewModel управляет всеми сервисами)
│   │   ├── MainWindowViewModel.cs
│   │   ├── MainWindowViewModel.Settings.cs
│   │   ├── VoiceBindingViewModel.cs
│   │   └── Wizard/          # VM мастера первого запуска
│   ├── Views/               # Avalonia AXAML представления (тонкие UI-оболочки)
│   │   ├── MainWindow.axaml
│   │   ├── PlatformTabsPanel.axaml
│   │   ├── VoiceSettingsPanel.axaml
│   │   ├── ModelControlPanel.axaml
│   │   ├── QueuePanel.axaml
│   │   ├── CachePanel.axaml
│   │   ├── SettingsPanel.axaml
│   │   └── Wizard/          # Представления мастера
│   ├── Services/            # Слой внешнего ввода-вывода
│   │   ├── TwitchService.cs          # TwitchLib WebSocket-клиент
│   │   ├── VKPlayService.cs          # Пользовательский WebSocket для VK Play
│   │   ├── QwenTtsClient.cs          # HTTP-клиент TTS API
│   │   ├── TtsConnectionManager.cs   # Проверки работоспособности, автоповтор
│   │   ├── AudioPlayer.cs            # Обёртка NetCoreAudio
│   │   └── GlobalHotkeyService.cs    # Системные горячие клавиши через SharpHook
│   ├── Managers/            # Ядро бизнес-логики
│   │   ├── MessageQueueManager.cs    # Извлечение голоса, очереди по голосам
│   │   ├── SynthesisOrchestrator.cs  # Цикл синтеза + цикл воспроизведения
│   │   ├── PlaybackController.cs     # Последовательное воспроизведение, микширование громкости
│   │   └── CacheManager.cs           # Дисковый WAV-кеш, вытеснение LRU
│   ├── Models/              # Модели данных
│   │   ├── AppSettings.cs            # JSON-схема настроек
│   │   ├── QueuedMessage.cs          # Конечный автомат сообщения
│   │   └── VoiceInfo.cs              # Метаданные TTS-голоса
│   ├── Infrastructure/      # Общие компоненты
│   │   ├── Constants.cs              # Константы приложения (без магических чисел)
│   │   ├── SettingsRepository.cs     # Загрузка/сохранение/валидация настроек
│   │   └── AppLoggerFactory.cs       # Конфигурация Serilog
│   ├── Converters/          # Конвертеры значений Avalonia
│   ├── Lang/                # Словари ресурсов i18n
│   │   ├── en.axaml
│   │   └── ru.axaml
│   └── Styles/              # Общие AXAML-стили
├── data/                    # Данные времени выполнения (создаются автоматически)
│   ├── settings.json
│   ├── cache/
│   │   ├── index.json
│   │   └── *.wav
│   └── logs/
└── docs/                    # Документация
    └── ru/
        └── README.md
```

### Ключевые архитектурные слои
- **Views** — Тонкие AXAML-оболочки, привязанные к ViewModels (без бизнес-логики)
- **ViewModels** — Слой оркестрации (MainWindowViewModel владеет всеми сервисами/менеджерами)
- **Services** — Внешний ввод-вывод (стриминговые платформы, TTS-сервер, воспроизведение аудио)
- **Managers** — Ядро бизнес-логики (очереди, синтез, воспроизведение, кеш)
- **Models** — Структуры данных и схема настроек

---

## Технологический стек

- **.NET 10** — Кроссплатформенная среда выполнения
- **Avalonia 11.3** — XAML-фреймворк UI с темой Fluent Dark
- **Semi.Avalonia** — Библиотека иконок и компонентов дизайна
- **CommunityToolkit.Mvvm** — Генераторы исходного кода для шаблонов MVVM
- **TwitchLib** — Twitch IRC/WebSocket-клиент
- **NetCoreAudio** — Кроссплатформенное воспроизведение аудио (`aplay` на Linux)
- **SharpHook** — Глобальные горячие клавиши через X11 на Linux, нативные хуки на Windows
- **Serilog** — Структурированное логирование с ежедневной ротацией файлов

---

## Сборка из исходников

```bash
# Клонировать репозиторий
git clone https://github.com/VirtualZer0/StreamTalkerClient.git
cd StreamTalkerClient

# Собрать проект
dotnet build src/StreamTalkerClient.csproj

# Запустить приложение
dotnet run --project src/StreamTalkerClient.csproj

# Опубликовать исполняемый файл для платформы
# Windows
dotnet publish src/StreamTalkerClient.csproj -c Release -r win-x64

# Linux
dotnet publish src/StreamTalkerClient.csproj -c Release -r linux-x64
```

---

## Участие в разработке

Вклад в проект приветствуется! Чтобы внести свой вклад:

1. **Форкните** репозиторий
2. **Создайте ветку функции** (`git checkout -b feature/your-feature`)
3. **Следуйте паттернам MVVM** — Держите Views тонкими, логику в ViewModels/Services/Managers
4. **Тестируйте на Windows и Linux** (при изменении платформенно-зависимого кода)
5. **Отправьте Pull Request** с чётким описанием изменений

### Стиль кода
- Используйте возможности C# 12 (namespaces уровня файла, record types, pattern matching)
- Следуйте существующей архитектуре (Services → Managers → ViewModels → Views)
- Добавляйте XML-комментарии для публичных API
- Используйте константы из `AppConstants` вместо магических чисел

---

## Лицензия

Этот проект лицензирован по **лицензии MIT**. Подробности см. в файле [LICENSE](../../LICENSE).

---

## Благодарности

- **[Qwen3-TTS](https://github.com/QwenLM/Qwen3-TTS)** за движок синтеза AI-голоса
- **[Qwen3-TTS-streaming](https://github.com/rekuenkdr/Qwen3-TTS-streaming)** за форк с оптимизациями потокового вывода
- **Semi Design** за библиотеку иконок
- **Сообщество Avalonia UI** за отличный кроссплатформенный фреймворк
